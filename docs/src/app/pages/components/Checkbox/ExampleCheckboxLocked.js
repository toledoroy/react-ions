import React from 'react'
import Checkbox from 'react-ions/lib/Checkbox'

const ExampleCheckboxLocked = () => (
  <Checkbox label='Locked checkbox' value={true} locked={true} />
)

export default ExampleCheckboxLocked
